[project]
name = "subscription-manager"
version = "0.1.0"
description = "Subscription management microservice for quant trading system"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
    "vulture>=2.16",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
