-- =============================================================================
-- 信号服务数据库迁移脚本（旧版，已集成到 01-database-init.sql）
-- 注意：此脚本已不再使用，strategy_signals 表由 01-database-init.sql 统一管理
-- 保留此文件仅用于历史参考
-- =============================================================================
--
-- 新的表结构请参考 01-database-init.sql 中的 strategy_signals 表定义
--
-- 关键变更：
-- - signal_id 字段已移除，id (BIGSERIAL) 作为唯一标识
-- - config_id 已重命名为 alert_id
-- - strategy_name 已重命名为 strategy_type
--
-- =============================================================================
